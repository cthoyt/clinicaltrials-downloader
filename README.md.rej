diff a/README.md b/README.md	(rejected hunks)
@@ -43,11 +43,11 @@ Download and process ClinicalTrials.gov
 
 ### Command Line Interface
 
-The clinicaltrials_downloader command line tool is automatically installed.
-It can be used from the console with the `--help` flag to show all subcommands:
+The `clinicaltrials_downloader` command line tool is automatically installed. It
+can be used from the console with the `--help` flag to show all subcommands:
 
 ```console
-clinicaltrials_downloader --help
+$ clinicaltrials_downloader --help
 ```
 
 > TODO show the most useful thing the CLI does! The CLI will have documentation
@@ -57,19 +57,34 @@ clinicaltrials_downloader --help
 
 <!-- Uncomment this section after your first ``tox -e finish``
 The most recent release can be installed from
-[PyPI](https://pypi.org/project/clinicaltrials_downloader/) with:
+[PyPI](https://pypi.org/project/clinicaltrials_downloader/) with uv:
 
 ```console
-python3 -m pip install clinicaltrials_downloader
+$ uv pip install clinicaltrials_downloader
+```
+
+or with pip:
+
+```console
+$ python3 -m pip install clinicaltrials_downloader
 ```
 -->
 
-The most recent code and data can be installed directly from GitHub with:
+The most recent code and data can be installed directly from GitHub with uv:
+
+```console
+$ uv --preview pip install git+https://github.com/cthoyt/clinicaltrials-downloader.git
+```
+
+or with pip:
 
 ```console
-python3 -m pip install git+https://github.com/cthoyt/clinicaltrials-downloader.git
+$ UV_PREVIEW=1 python3 -m pip install git+https://github.com/cthoyt/clinicaltrials-downloader.git
 ```
 
+Note that this requires setting `UV_PREVIEW` mode enabled until the uv build
+backend becomes a stable feature.
+
 ## 👐 Contributing
 
 Contributions, whether filing an issue, making a pull request, or forking, are
@@ -159,11 +176,11 @@ More info on Cruft's update command is available
 ### 🥼 Testing
 
 After cloning the repository and installing `tox` with
-`python3 -m pip install tox tox-uv`, the unit tests in the
-`tests/` folder can be run reproducibly with:
+`uv tool install tox --with tox-uv` or `python3 -m pip install tox tox-uv`, the
+unit tests in the `tests/` folder can be run reproducibly with:
 
 ```console
-tox -e py
+$ tox -e py
 ```
 
 Additionally, these tests are automatically re-run with each commit in a
@@ -256,13 +273,12 @@ Note that this deprecates previous workflows using `.pypirc`.
 
 #### Uploading to PyPI
 
-After installing the package in development mode and installing
-`tox` with
-`python3 -m pip install tox tox-uv`, run the following from
-the console:
+After installing the package in development mode and installing `tox` with
+`uv tool install tox --with tox-uv` or `python3 -m pip install tox tox-uv`, run
+the following from the console:
 
 ```console
-tox -e finish
+$ tox -e finish
 ```
 
 This script does the following:
@@ -282,8 +298,7 @@ This script does the following:
 
 #### Releasing on GitHub
 
-1. Navigate to
-   https://github.com/cthoyt/clinicaltrials-downloader/releases/new
+1. Navigate to https://github.com/cthoyt/clinicaltrials-downloader/releases/new
    to draft a new release
 2. Click the "Choose a Tag" dropdown and select the tag corresponding to the
    release you just made
